{% set name = "fractal-task-tools" %}
{% set version = "0.3.0" %}
{% set python_min = "3.11.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/fractal_task_tools-{{ version }}.tar.gz
  sha256: 5f9243108d0974c22ea04267a7dd5f240c2af8fd1516b5bd5c3c2fa45b5ecc9d

build:
  entry_points:
    - fractal-manifest = fractal_task_tools._cli:main
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 1

requirements:
  host:
    - python {{ python_min }},<=3.13.0
    - setuptools >=80.0
    - pip
  run:
    - python >={{ python_min }},<=3.13.0
    - pydantic >=2.6.0,<2.13.0
    - docstring_parser >=0.15,<=0.17
    - packaging

test:
  imports:
    - fractal_task_tools
  commands:
    - pip check
    - fractal-manifest --help
  requires:
    - pip
    - python {{ python_min }}

about:
  summary: Shared tools for Fractal tasks
  home: https://github.com/fractal-analytics-platform/fractal-task-tools
  license: BSD-3-Clause
  license_file: LICENSE

extra:
  recipe-maintainers:
    - mfranzon
    - jluethi
    - tcompa
